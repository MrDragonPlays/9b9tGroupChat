package net.cb2k.GroupChat.commands;

import org.bukkit.command.Command;
import org.bukkit.command.CommandExecutor;
import org.bukkit.command.CommandSender;

public class GroupKickCommand implements CommandExecutor {

    @Override public boolean onCommand(CommandSender commandSender, Command command, String s, String[] strings) {

        // TODO Check if player is in group

        // TODO Check if player has permission

        // TODO Check if target is in in group

        // TODO Remove target from group object

        // TODO Remove group from target object

        return true;
    }

}
